<% post_images.each do |post_image| %>
  <div>
    <%= link_to post_image_path(post_image.id) do %>
      <%= image_tag post_image.image.attached? ? post_image.image : 'no_image.jpg' %>
    <% end %>
    <p>投稿ユーザー画像：
    <%= image_tag post_image.user.profile_image.attached? ? post_image.user.profile_image : 'no_image.jpg', size: "60x60" %>
    </p>
    <p>ショップ名：<%= post_image.shop_name %></p>
    <p>説明：<%= post_image.caption %></p>
    <p>ユーザーネーム：<%= post_image.user.name %></p>
    <p><%= link_to "#{post_image.post_comments.count} コメント", post_image_path(post_image.id) %></p>
  </div>
  <% end %>
  
  <%= paginate @post_images %>